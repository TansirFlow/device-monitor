# 安全模型

> **结论先说**：主控（master）在架构上**不具备**操作节点服务器的任何手段。
> 这不是"我们没做这个功能"，而是"这个功能所需要的基础设施在这个系统里根本不存在"。

---

## 1. 数据流只有一条单向通道

```
   ┌─────────────────────┐                        ┌─────────────────────┐
   │   节点 A            │                        │                     │
   │  ┌───────────────┐  │   HTTPS POST（仅出站） │   主控 master       │
   │  │ mon-agent     │──┼───────────────────────▶│  ┌───────────────┐  │
   │  │ 无监听端口     │  │  Authorization: Bearer │  │ 接收 + 存储    │  │
   │  │ 无命令通道     │  │  body = 只读指标 JSON   │  │ 只读看板       │  │
   │  └───────────────┘  │                        │  └───────────────┘  │
   │        ▲            │      ✗ 没有反向通道 ✗   │        │            │
   │        └────────────┼────────────────────────│◀───────┘            │
   │        不存在这条线   │                        │                     │
   └─────────────────────┘                        └─────────────────────┘
```

- 节点 → 主控：一条 HTTPS POST，携带一个只读 JSON。
- 主控 → 节点：**没有**。没有监听端口、没有长连接、没有消息队列、没有轮询指令的接口。

---

## 2. 八条结构性保证

| # | 保证 | 实现位置 | 破坏它需要什么 |
|---|------|----------|----------------|
| 1 | agent **不监听任何端口** | 全代码库无 `net.Listen` / `ListenAndServe` / `ListenPacket` | 新增监听代码（会被 `make check` 拦下） |
| 2 | agent **不注册任何服务端处理器** | 无 `http.Server`、`ServeMux`、`http.HandleFunc` | 同上 |
| 3 | agent **不解析主控响应** | `sender.go` 只读状态码，响应体 `io.Copy(io.Discard, …)` 丢弃 | 新增响应解析逻辑 |
| 4 | agent **不写任何文件** | 无 `os.Create` / `os.WriteFile` / `os.OpenFile`；断网缓冲只在内存里 | 新增落盘逻辑 |
| 5 | agent **不执行来自外部的命令** | 唯一的 `exec` 在 `gpu.go`，参数是编译期常量 `nvidia-smi --query-gpu=…`，不含任何变量插值 | 让命令参数来自配置/网络 |
| 6 | agent **不跟随重定向** | `CheckRedirect: return http.ErrUseLastResponse` | 去掉该行（会被审计拦下） |
| 7 | master **没有任何出站网络能力** | 全代码库无 `http.Get/Post/NewRequest`、无 `net.Dial`、无 `exec` | 新增任何外连代码 |
| 8 | master **不执行任何外部命令** | 全代码库无 `os/exec` | 新增 exec 代码 |

第 7 条是这套设计的核心：**主控连"往外发一个请求"这个动作都做不到**。
所以即使主控主机被完全攻陷，攻击者拿到的是一个"只能读磁盘上已有指标、只能应答 HTTP 请求"的进程。
它没有任何可用的原语去联系节点。

### 关于唯一的那个 `exec`

`agent/gpu.go` 调用 `nvidia-smi` 是采集 GPU 指标最轻量的方式（不引入 NVML 的 cgo 依赖，从而保住静态编译和 6MB 体积）。
它的安全性建立在两点上：

```go
// 参数是编译期常量，没有任何来自配置、网络或环境的插值
const query = "index,name,uuid,utilization.gpu,memory.used,memory.total,temperature.gpu,power.draw,fan.speed"
args := []string{"--query-gpu=" + query, "--format=csv,noheader,nounits"}
```

1. 参数向量恒定，攻击者无法通过上报链路影响执行什么；
2. 执行上下文带 5 秒超时，失败只记一条降级信息，不影响其他指标。

如果你所在的合规环境要求"零 exec"，把 `collect.gpu` 和 `gpu.enabled` 设为 `false` 即可 ——
此时 `os/exec` 的代码路径完全不会被走到。

**温度采集没有新增任何 exec**（这一点很重要，因为 Windows 上读 CPU 温度最省事的做法就是
调一次 `powershell Get-CimInstance`）：温度全部来自 Linux 的只读 sysfs 文件、
Windows 的 `IOCTL_STORAGE_QUERY_PROPERTY` 设备控制码，以及已经存在的那个 nvidia-smi 调用。
审计里"外部命令只允许出现在 gpu.go"这条断言仍然成立，没有被为了功能而放宽。

**CPU 拓扑采集（物理核心数 / 线程数）同样没有新增任何 exec。** 物理核心数在 Linux 上来自
只读 sysfs（`/sys/devices/system/cpu/cpuN/topology/`），Windows 上来自 kernel32 的只读查询
`GetLogicalProcessorInformationEx`。Windows 上更省事的写法是调 `wmic cpu get NumberOfCores`
或 `powershell Get-CimInstance Win32_Processor`，但那会再次打破上面这条约束，因此没有采用。

---

## 3. 威胁模型

| 攻击者位置 | 能做到 | **做不到** |
|---|---|---|
| 主控主机被完全攻陷（root） | 读走磁盘上已有的全部指标；伪造/丢弃后续指标 | ❌ 在节点上执行任何命令<br>❌ 读取节点上的任何文件<br>❌ 修改节点配置<br>❌ 让节点建立任何回连<br>❌ 让节点下载任何东西 |
| 传输链路中间人（持有 TLS 私钥） | 篡改、重放、丢弃上报数据 | ❌ 上述全部（报文里没有可执行的字段） |
| 拿到某个节点的 `agent.json` | 以**该节点身份**上报伪造数据 | ❌ 影响其他节点<br>❌ 让主控做任何操作 |
| 拿到 `admin_token` | 查看全部看板数据、导出历史 | ❌ 写入任何节点<br>❌ 修改任何节点数据 |
| 拿到某节点的上报令牌 | 以该节点身份写入数据（仅该节点） | ❌ 冒充其他节点（`node_tokens` 模式下令牌与 node_id 强绑定） |
| 已入侵的节点侧攻击者 | 上报伪造指标刷爆主控磁盘 | ❌ 通过主控影响其他节点（已限流 + 体积上限 + 保留期清理） |

### 为什么"报文里没有可执行字段"这句话是成立的

主控收到的 JSON 里所有字段只被两处使用：

1. **展示** → 看板渲染前统一做 HTML 转义（`web/app.js` 的 `esc()`）；
2. **存储路径** → 只有 `node_id` 参与路径构造，且两侧都过 `^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$` 白名单，
   再叠加 `within()` 校验最终路径不越出数据目录。

除此之外没有任何字段被当作命令、URL、路径或回调地址使用。

温度传感器带入了两个**新的、节点可控的字符串字段**（`name`、`source`），这里单独说明它们的处理：

- 两者只在看板上以文本形式展示，渲染前一律走 `esc()` 转义，不参与任何 DOM 结构构造；
- `kind` 字段虽然会拼进查询参数名（`metric=temp:<kind>`），但它只被用于
  **与传感器列表里的 `kind` 做字符串相等比较**，不参与路径拼接、不参与命令构造。
  主控还对其做了字符集限制（只允许小写字母、数字、下划线，长度 ≤ 24）；
- 传感器列表本身有数量上限（32）与取值区间过滤（`0 < temp_c ≤ 150`），
  因此不存在"用温度字段把主控撑爆"或"塞入 255 伪造过热"的路径。

---

## 4. 自动化审计：把保证变成可执行断言

```
make check
```

会依次跑 `go vet`、两个包各自的单元测试、`scripts/security-audit.sh`
以及前端渲染冒烟测试。审计脚本对源码做 18 项静态断言，分成三类：

**「不得存在」**（agent 不得监听端口、不得写文件、不得删除文件；master 不得 exec、不得外连）
**「只能出现在白名单文件」**（`os/exec` 只允许在 `gpu.go`；`net/http` 只允许在 `sender.go`）
**「必须存在」**（必须拒绝重定向、必须有 node_id 白名单、必须恒定时间比较令牌、
必须限制请求体大小、必须限制温度指标名字符集）

任何一次提交破坏其中一条，`make check` 直接失败。
这比"口头承诺"有用的地方在于：它是可执行的、会被 CI 卡住的。

**审计范围只包含会进二进制的源码，`*_test.go` 被显式排除**（`--exclude=*_test.go`）：

- 测试代码不参与 `go build`，所以它在临时目录里造 fixture、写文件，
  不构成对「agent 不写文件」这条保证的破坏；
- 反过来，「必须存在」这类断言也不能被测试文件里的字符串满足 ——
  否则往测试里抄一句正则文本就能让审计误判为通过。
  排除测试文件让这类断言变得更严格。

```
$ make check
==> go vet
==> 单元测试
ok  	monagent
ok  	monmaster
==> 安全审计
== 结构性约束审计 ==
-- agent：必须是一个纯粹的单向上报端 --
  [通过] agent 不得监听任何端口（否则主控就有了连入点）
  ...
结论：全部通过 —— 主控在架构上不具备操作节点服务器的手段。
```

---

## 5. 部署加固清单

内置的两份 systemd 单元已经把能关的都关了（`ProtectSystem=strict`、`CapabilityBoundingSet=` 为空、
`SystemCallFilter=@system-service`、`RestrictAddressFamilies`、内存/CPU 上限、非 root 专用用户）。
**因为 agent 运行期不写任何文件，所以可以安全地把它跑在只读根文件系统上。**

除此之外建议：

### 网络方向

- 节点侧出向策略：只允许 `tcp/443` 到主控域名，其余一律拒绝。
- 主控侧入向策略：`/api/v1/report` 只对节点网段开放；看板只对办公网开放。
- **不要**给节点开放任何入向端口 —— 这套系统不需要。

### 传输

- 必须走 HTTPS。agent 会对 `http://` 直接启动失败，除非显式设置 `MON_ALLOW_INSECURE_HTTP=1`。
- 内网自签场景：用 `tls.ca_file` 指定 CA，**不要**用 `insecure_skip_verify`（联调例外）。
- 追求更强互信可以开 mTLS（配置 `tls.cert_file` / `tls.key_file`）。

### 凭据

- 用 `node_tokens` 做一节点一令牌，而不是共用 `report_token`。
  这样单个节点泄露的令牌无法污染其他节点的数据。
- 令牌生成：`openssl rand -hex 24`（后台里点"随机生成"是等价的 32 字节随机串）。
- 配置文件权限 `600`，属主 root。agent 启动时若发现权限过宽会打印告警。
- `admin_token` 与上报令牌**必须不同**。
- **管理后台账号**又是另一套凭据（见第 6 节）：口令用 PBKDF2-HMAC-SHA256
  （21 万次迭代 + 16 字节随机盐）存储，明文从不落盘。

### 反向代理

- master 默认只监听 `127.0.0.1`。如果 `listen` 不是回环地址而 `admin_token` 为空，
  master 会**拒绝启动**（除非显式设置 `MON_MASTER_ALLOW_OPEN_READ=1`）。
- 参考 `deploy/nginx.conf.example`：TLS 终止、限流、请求体上限、方法白名单、可选的 IP 白名单。

---

## 6. 管理后台（账号、令牌、一键安装）

这一节说明：给主控加一个 Web 后台之后，前面那些保证**为什么依然成立**。

### 6.1 单向性没有被破坏

一键安装的实现是"节点来取，主控应答"：

```
节点:  curl .../api/v1/install.sh?node=X&code=Y   →  主控返回填好令牌的脚本
节点:  curl .../api/v1/agent/mon-agent-linux-amd64  →  主控返回二进制
```

主控依然没有主动发起过任何连接 —— 审计脚本里那条
"master 不得主动发起任何网络连接"的断言照旧通过。
主控**不会**去连节点、不会推送配置、不会触发任何远程动作。

### 6.2 三类凭据，权限严格分离

| 凭据 | 能做什么 | 不能做什么 |
|---|---|---|
| `report_token` / `node_tokens` | 写入自己那个节点的指标 | 读不到任何数据；也写不了别的节点 |
| `admin_token` | 读看板与查询接口 | 不能写；不能进管理后台 |
| 后台账号（会话） | 管理节点与令牌 | 与上报/读取令牌互不通用 |

不存在任何"既能读又能写"或"一个令牌打通全部"的凭据。

### 6.3 会话与 CSRF

- 会话是自包含的 HMAC 签名串（`v1.<过期>.<用户名>.<签名>`），密钥由**口令哈希**派生：
  改密码 → 所有会话立即失效；重启 → 会话仍然有效（不把密钥放内存随机值里，免得重启就掉线）。
- Cookie：`HttpOnly` + `SameSite=Strict`，HTTPS 下加 `Secure`。
- 写接口额外要求一个自定义请求头 `X-Mon-Csrf: 1`。HTML 表单设不了请求头，
  跨站 `fetch` 会被 CORS 预检挡住 —— 因此没有 CSRF 面。
- 登录入口单独限流（突发 10 次后每 10 秒一次）；用户名错与口令错返回同一句话。

### 6.4 令牌存哪、为什么

节点令牌与安装码落在 `<data_dir>/nodes.json`（600），**主控永不改写自己的 `master.json`**：

- 避免"主控改写自己配置文件"带来的并发写坏、文件锁、以及与 `MON_MASTER_*` 环境变量覆盖冲突；
- 主控唯一的可写目录是 `data_dir`，systemd 单元也只放开了它，落在这里不需要放宽加固；
- 代价是**备份必须连 `data_dir` 一起做** —— 只备份配置文件会丢掉全部后台创建的令牌。

### 6.5 安装码与上报令牌分离

安装码只用于换取安装脚本，独立于上报令牌：

- 轮换 / 撤销安装码**不影响**已经在正常上报的节点；
- 一键命令里只有 `node` 与 `code`，没有令牌 —— 贴到工单里的爆炸半径是一个节点，
  且随时可撤销；
- 脚本本身含令牌，所以脚本头部写明"当作密钥处理"，且二进制下载也要校验安装码
  （否则这个端点就成了公开的匿名文件服务）。

### 6.6 已知取舍

- PBKDF2 是**手写**的（标准库到 Go 1.24 才有 `crypto/pbkdf2`，而本项目要求 Go 1.21+ 且零第三方依赖）。
  它只是"迭代 HMAC"，实现很短，并有与 Python `hashlib.pbkdf2_hmac` 对拍的测试向量
  （含 40 字节跨块与 UTF-8 中文口令）兜底。
- 会话是签名的、无状态的，**不支持单点踢下线**（只能靠改密码让全部会话失效）。
- 只有一个管理员账号，没有角色/多用户。够用，也少一个攻击面。

---

## 7. 已知边界（不做的和做不到的）

- **agent 不能自我更新**。升级需要人工替换二进制。这是特性不是缺陷 ——
  自更新通道等于给主控（或拿到主控的人）一个在节点上执行代码的手段。
- **agent 不拉取配置**。所有配置都在本地文件里。
- **master 无法主动触发节点上报**。指标是节点推的，master 只能等。
- **断网期间的数据保存在内存中**，默认最多 120 条（`buffer_max`），进程重启即丢失。
  这是为了坚守"agent 不写文件"。需要更长离线缓存的话，请自行评估落盘带来的攻击面。
- **单主控**。没有集群、没有节点间通信。master 之间也不互相通信。
- Windows 节点暂不支持网络吞吐采集（`net` 指标为空），其他指标正常。
- **Windows 节点读不到 CPU / 主板 / 网卡温度**。这是平台限制：Windows 没有公开的、
  非管理员可用的 CPU 温度接口，可行方案只有 WMI（需要 COM 互操作）、内核驱动
  （OpenHardwareMonitor 之类）或调用 `powershell Get-CimInstance`。
  前两者引入依赖与新的攻击面，第三者会打破「外部命令只允许出现在 gpu.go」这条结构性保证。
  **这个项目选择不报温度，而不是为了功能放宽安全边界。**
  磁盘温度走 `IOCTL_STORAGE_QUERY_PROPERTY`（需要管理员权限），非管理员运行时静默跳过。
- **容器内需要挂载 `/sys`** 才能读到宿主机的温度传感器
  （`docker run -v /sys:/sys:ro` 或 `--device`）。否则会收到一条采集降级提示。
- **温度读数是"尽力而为"**：能不能读到取决于芯片、驱动和内核版本。
  读不到时既不报错误值也不编造，传感器直接不出现在列表里。

---

## 8. 如果你以后想加"远程操作"功能

**请不要加在这个项目里。**

这个项目的全部价值来自于"主控做不到某件事"这个可验证的性质。
一旦加入任何形式的下行通道（命令、脚本下发、文件推送、配置热更新、重启指令），
上面 7 条保证会一次性全部失效 —— 因为节点将不得不开始信任主控发来的内容。

如果业务上确实需要远程运维，正确做法是引入一个**独立的、面向运维人员的、有审批流的**
工具（如 Ansible / SSH 跳板机 / 带审计的运维平台），让它与监控系统在
**凭据、网络路径、审计日志**三个维度上完全隔离。

监控系统不应该成为运维通道。这是它安全的前提。
